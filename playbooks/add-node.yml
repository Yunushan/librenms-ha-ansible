---
# Usage:
# 1. add the host to active inventory groups
# 2. optionally keep it in new_nodes while testing
# 3. run this playbook
- name: Apply site playbook for add-node workflow
  ansible.builtin.import_playbook: site.yml
