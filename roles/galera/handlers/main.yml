---
- name: Clear stale Galera readiness agent workers
  ansible.builtin.command:
    argv:
      - /usr/local/sbin/librenms-galera-readiness-agent-reset
  changed_when: false
  when:
    - librenms_db_mode == 'galera'
    - librenms_galera_readiness_agent_enabled | bool

- name: Restart Galera readiness agent socket
  systemd:
    name: "{{ librenms_galera_readiness_agent_socket_unit }}"
    state: restarted
    daemon_reload: true
  when:
    - librenms_db_mode == 'galera'
    - librenms_galera_readiness_agent_enabled | bool

- name: Start persistent Galera readiness agent server after socket restart
  systemd:
    name: "{{ librenms_galera_readiness_agent_service_unit }}"
    state: started
  when:
    - librenms_db_mode == 'galera'
    - librenms_galera_readiness_agent_enabled | bool
